{{- if .Values.mapOperator.secrets.sealedSecrets.enabled }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ include "map-operator.fullname" . }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "map-operator.labels" . | nindent 4 }}
  annotations:
    sealedsecrets.bitnami.com/managed: "true"
spec:
  encryptedData:
    AWS_ACCESS_KEY_ID: {{ .Values.mapOperator.secrets.sealedSecrets.encryptedData.awsAccessKeyId }}
    AWS_SECRET_ACCESS_KEY: {{ .Values.mapOperator.secrets.sealedSecrets.encryptedData.awsSecretAccessKey }}
  template:
    metadata:
      name: {{ include "map-operator.fullname" . }}-secrets
      namespace: {{ .Release.Namespace }}
    type: Opaque
{{- end }}