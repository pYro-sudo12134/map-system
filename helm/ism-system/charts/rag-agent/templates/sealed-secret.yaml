{{- if .Values.agent.secrets.sealedSecrets.enabled }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ include "rag-agent.fullname" . }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rag-agent.labels" . | nindent 4 }}
  annotations:
    sealedsecrets.bitnami.com/managed: "true"
spec:
  encryptedData:
    AWS_ACCESS_KEY_ID: {{ .Values.agent.secrets.sealedSecrets.encryptedData.awsAccessKeyId }}
    AWS_SECRET_ACCESS_KEY: {{ .Values.agent.secrets.sealedSecrets.encryptedData.awsSecretAccessKey }}
  template:
    metadata:
      name: {{ include "rag-agent.fullname" . }}-secrets
      namespace: {{ .Release.Namespace }}
    type: Opaque
{{- end }}