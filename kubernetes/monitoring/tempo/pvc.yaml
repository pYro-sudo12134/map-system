apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tempo-local-pvc
  namespace: monitoring
spec:
  storageClassName: zfs-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi