apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - statefulset.yaml
  - service.yaml
  - pvc.yaml
  - configmap.yaml

#secretGenerator:
#  - name: loki-secret
#    envs:
#      - .env

generatorOptions:
  disableNameSuffixHash: true

commonLabels:
  app: loki
  component: logging