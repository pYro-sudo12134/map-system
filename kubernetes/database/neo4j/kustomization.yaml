apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: database

resources:
  - pdb.yaml
  - pvc.yaml
  - service.yaml
  - statefulset.yaml

commonLabels:
  app: neo4j
  component: database