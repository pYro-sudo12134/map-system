apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: database

resources:
  - statefulset.yaml
  - service.yaml
  - pvc.yaml
  - configmap.yaml
  - pdb.yaml

generatorOptions:
  disableNameSuffixHash: true

commonLabels:
  app: postgres
  component: database