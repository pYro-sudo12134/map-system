apiVersion: v1
kind: Service
metadata:
  name: map-operator
  namespace: ism-system
  labels:
    app: map-operator
spec:
  selector:
    app: map-operator
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090