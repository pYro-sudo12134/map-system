apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ism-cert
  namespace: ingress
spec:
  secretName: ism-tls-secret
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - api.change.me
    - "*.change.me"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer